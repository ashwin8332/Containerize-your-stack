# Copy this file to .env and fill in your values.
# When running via docker compose, the DB host is the service name "db".
# When running the app locally (outside Docker), use localhost.
DATABASE_URL=postgres://postgres:postgres@db:5432/appdb
PORT=3000
